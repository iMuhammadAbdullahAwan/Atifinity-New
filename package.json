{
  "name": "atifinity",
  "version": "2.0.0",
  "description": "Atifinity — Creative. Connected. Limitless. YouTube growth, built end-to-end.",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i ./src/css/input.css -o ./dist/css/output.css --minify",
    "build:js": "esbuild src/js/main.js src/js/modules/*.js --minify --outdir=dist/js --outbase=src/js",
    "build:bundle": "node scripts/bundle-js.mjs",
    "build": "npm run build:css && npm run build:js && npm run build:bundle"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@tailwindcss/cli": "^4",
    "esbuild": "^0.28.2",
    "tailwindcss": "^4"
  }
}
